# Audit runs ARE tracked in this repo — see audits/README.md.
#
# This was deliberately reversed on 10 Sep 2026: the repo is a working
# archive of client audits, and a before/after across two dated runs is the
# most useful thing in it. Keep the repo PRIVATE. A run directory describes
# unfixed findings on somebody's live site, and that is not something to
# publish while it is still unfixed.

node_modules/
*.local/

# Incidental
.DS_Store
npm-debug.log*
